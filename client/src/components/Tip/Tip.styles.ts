import { createStyles } from '@material-ui/core/styles';

const styles = () => createStyles({
    root: {
        color: '#CDCDCD',
        fontSize: '0.95rem'
    },

    icon: {
        fontSize: '1.3rem',
        marginRight: '0.5rem'
    }
});

export default styles;